config:
  enabled: true
  clients:
    - url: http://loki-gateway/loki/api/v1/push

serviceMonitor:
  enabled: true
  namespace: monitoring
  interval: 30s
  scrapeTimeout: 10s
  labels:
    release: prometheus